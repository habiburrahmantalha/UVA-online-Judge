#include<stdio.h>
#include<string.h>
#include<math.h>
#include<string>
#include<vector>
#include<map>
#include<algorithm>
#include<iostream>
#include<queue>
#include<set>
using namespace std;
#define PI 2 * acos (0.0)


int main()
{
    while(cin>>s)
    {
        if(s=="#")
        break;
        m[s]=1;
    }
    getline(cin,s);
    getline(cin,s);
    for(i=0;i<=26;i++)
    {
        string t+"";
        for(j=0;j<s.length();j++)
        {
            t+=(s[i]-'A'+1+i)%27;

        }
        ss
    }
    return 0;
}
