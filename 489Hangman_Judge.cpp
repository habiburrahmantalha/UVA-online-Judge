#include<stdio.h>
#include<string>
#include<string.h>

using namespace std;

int main()
{
    while(scanf("%d",&r) && r!=-1)
    {
        cin>>puzzle;
        cin>>guess;

        for(i=0;i<guess.size();i++)
        {
            if()
        }
    }
}
